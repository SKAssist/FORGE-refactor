/**
 * @file ollamaAgent.ts
 * @description Contains a helper function to interact with the Ollama API (running locally).
 * Sends prompts and receives completions from the LLaMA model hosted on localhost:11434.
 *
 * @usedBy
 * - refactorWithVerification.ts
 * - crossFileChange.ts (future)
 */
